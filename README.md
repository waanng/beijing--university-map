# 北京高校探访路线规划网站

这是一个纯静态网站，用于展示北京 550 分以上高校的地理位置、录取分数、优势专业和校园图片，并帮助学生规划线下探访路线。

## 本地预览

在本目录启动 HTTP 服务：

```bash
python3 -m http.server 8290
```

然后打开：

```text
http://localhost:8290/
```

请不要直接用 `file://` 打开，浏览器可能会阻止 JSON 数据读取。
