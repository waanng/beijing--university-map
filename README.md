# 北京高校探访筛选与对比网站

这是一个纯静态网站，用于展示北京 550 分以上高校的地理位置、录取分数、优势专业和校园图片，并帮助学生从分数、专业方向和区域等维度筛选值得实地探访的学校。

## 本地预览

在本目录启动 HTTP 服务：

```bash
python3 -m http.server 8290
```

然后打开：

```text
http://localhost:8290/
```

请不要直接用 `file://` 打开，浏览器可能会阻止 JSON 数据读取。
